# Reference-only clones, not part of this project's own history.
# See README.md "Project Layout" — firmware/ pulls Arduino_GFX from the
# PlatformIO registry, not these local copies.
/Arduino_GFX/
/rp2040-doom-LCD/

# Dump of the original controller's video assets (hundreds of MB of .amv
# files) — reference material for reverse-engineering, not source.
/original files/

__pycache__/
